 320x180 screen size
40x23 tiles (not full)

384x216
48x27

ASDF - four primary weapons
V - grenade launcher
Shift - switch gun to terraform and vice versa
space or < - switch to next gun

terraform gun should have limited uses?
replace raycast up for player with area2d cause it will never cover the entire head of player
while we're at it, the platforms dont seem to interact too well with it
make jump feel less heavy + remake the first level since you can now reach the hut

f1 - save at current location
f2 - noclip on/off
f3 - godmode on/off

todo til v0:
X add frog animations
X add double jump as an item
X make it possible to leave the office lore room
- add dialogue to radio in tower
X make boss room open only after picking up double jump
X rewrite switcher area as generic area that fires event on enter

code cleanliness todo:
X limit amount of export - replace with const and readonly
X remove all manual state changes and replace them with triggers
X prevent event memory leaks on events - make state exit on death?
X change controller code to be event-based instead of polling (_input instead of _update)
X add mediator class for inputs instead of relying on player to pass gui and interacted events 
X use float extension IsWithin for old code too
- find way to reuse states like in a decorator pattern
- implement deflect for gravity bullets
- make flip automatically look for hitboxes and flip them
- save DialoguesGlobalState as a whole instead of relying on one million SaveEntity components

todo balance:
- make zombie's jump / detection avoidable with double jump, but not with normal jump
- give rifle a long reload time
- make frog boss rain frogs instead of spawning them with a long attack - the hit frame should be at when he is charging for jump instead

todo would be nice:
- upgrade for blowtorch turning it into a shotgun or flamethrower type of weapon
- allow player to pick up any vine?
- allow for multiple markers per cell. in metsys store markers as a bitmask, and in map view if there are multiple symbols show them one by one interchangeably (blinking)
- lore-wise the teleporter kills the old you every time you use it
- some area that is only accessible once you discover a teleporter into it via some map item

todo bugs:
X lots of player bugs when walking through levels up/down. Make sure level is loaded before applying extra player velocity
- theres still a lot of bumping like issues when going up
- non-ase sprites don't ignore colors in blink shader

to-finish todo:
- finish terraforming sprayer for water (there's a sprite). It should be connected via fat pipes to water and spray terraforming there
X make collectibles display on map and mark themselves as collected dynamically
X make pickupable item use metsys storage instead of SaveEntity component